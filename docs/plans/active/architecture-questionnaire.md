# Architecture question status

The questionnaire is complete. Confirmed choices and inferred engineering
defaults live in [`architecture-decisions.md`](architecture-decisions.md).

## Current blockers

No question blocks planning or development.

## Deferred launch gate

Before public link submission is enabled, choose a monthly provider-cost alert
and a hard stop. Development may continue without them. Reads remain available
if the eventual hard stop disables new provider-costing work.

This is deliberately deferred because real per-platform cost has not been
measured. The implementation plan includes the measurement and requires this
decision before public writes.
